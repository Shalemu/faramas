import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xEF0047AB);
  static const primaryLight = Color(0xEF4F89DC);
  static const Color secondary = Colors.grey;
  static const greyLight = Color(0xD3FFFFFF);

  // Status colors
  static const Color success = Colors.green;
  static const Color warning = Colors.orange;
  static const Color error = Colors.red;

  // Text colors
  static const Color textPrimary = Colors.black;
  static const Color textDark = Colors.black;
  static const Color grey = Colors.grey;
  static const Color textSecondary = Colors.grey;
  static const Color textLight = Colors.white;

  // Background colors
  static const Color background = Colors.white;
  static const Color backgroundLight = Color(0xFFF5F5F5);

  // Card colors
  static const Color cardBackground = Colors.white;
  static const Color cardShadow = Color(0x1F000000);

  // Icon colors
  static const Color iconPrimary = Color(0xFFFFC107);
  static const Color iconSecondary = Colors.grey;

  // Button colors
  static const accent = Color(0xFFFFC107);
  static const Color buttonSecondary = Colors.blue;
}
